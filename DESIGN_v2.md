---
version: prototype-2026-v2
name: Triplog Design System v2
description: 마이페이지 개편 제안 — 틸(Teal) 히어로 + 다크 메뉴 존 모바일 인터페이스
source_of_truth: css/tokens.css + .my-v2 스코프
extends: DESIGN.md
---

# Triplog 디자인 규칙 v2 (마이페이지 개편)

`DESIGN.md`(블루 기반 Triple)를 **유지**한 채, 마이페이지 리디자인 시안을 위한 **v2 규칙**입니다.  
레퍼런스 시안(틸 프로필 카드 · 2×2 통계 · 다크 하단 메뉴)을 바탕으로 프로토타입에 맞게 소폭 조정했습니다.

> **범위:** MY-01 마이페이지 및 관련 토큰  
> **비교:** `index.html` 쇼케이스에서 v1(DESIGN.md) / v2(본 문서)를 나란히 확인

---

## 1. v1 → v2 변경 요약

| 항목 | v1 (`DESIGN.md`) | v2 (본 문서) |
|------|------------------|--------------|
| 브랜드 포인트 | 블루 `#368FFF` | 틸 `#2BB7B7` |
| 프로필 히어로 | 좌측 아바타 + 그라데이션 블루 카드 | **중앙 아바타** + 솔리드 틸 카드 |
| 경험치 | 프로필 카드 **내부** | 프로필 카드 **아래** 독립 블록 |
| 통계 | 아이콘+수치 가로형 2열 | 체크 아이콘 원형 + 수치 **2×2 그리드** |
| 하단 메뉴 | 밝은 surface 위 흰 카드 | **다크 존**(`#121416`) 위 흰 리스트 |
| 랭크 표기 | Score 행 안 Gold pill | Gold Rank pill + **월간 순위·점수** |

---

## 2. 제품 톤 & 원칙 (v2 추가)

| 원칙 | 설명 |
|------|------|
| **히어로 집중** | 첫 뷰포트는 틸 프로필 카드가 브랜드·성장 상태를 한눈에 전달 |
| **존 분리** | 상단(밝은 수집·기록) / 하단(다크 활동·설정)으로 스크롤 리듬 분리 |
| **보더 우선** | v1과 동일 — 카드는 `1px solid` 보더, 무거운 그림자 지양 |
| **터치 피드백** | `is-pressable` + `:active` 스케일 유지 |
| **모바일 1열** | 프레임 `390×844` 기준 동일 |

---

## 3. 색상 (v2 토큰)

v2는 글로벌 `tokens.css`를 깨지 않도록 **`.my-v2` 스코프**에서 오버라이드합니다.

### 브랜드 & 액센트

| 토큰 (스코프) | 값 | 용도 |
|---------------|-----|------|
| `--my-v2-primary` | `#2BB7B7` | 프로필 카드, XP 바, 체크 아이콘, 로그아웃 강조 |
| `--my-v2-primary-dark` | `#229A9A` | `:active` 눌림 |
| `--my-v2-primary-soft` | `#E6F7F7` | 통계 아이콘 원형 배경, soft 버튼 |
| `--my-v2-gold` | `#E8B84A` | Gold Rank pill (v1 `--accent-gold`와 동일) |
| `--my-v2-ink` | `#121416` | 하단 다크 존 배경 |
| `--my-v2-ink-label` | `#8B9198` | 다크 존 섹션 라벨 |

### 표면 & 텍스트

v1 토큰을 재사용합니다: `--white`, `--surface`(`#F5F6F7`), `--text`, `--text-secondary`, `--border`.

**규칙:** 앱 전역 primary를 틸로 바꾸지 않는다. 마이페이지 v2 블록 안에서만 틸을 쓴다.

---

## 4. 타이포그래피

v1과 동일 — `Pretendard`, `--text-base` 15px.

| 용도 | 크기 / weight |
|------|----------------|
| 닉네임 (히어로) | 22–24px / 800, 흰색 |
| 레벨·월간 순위 | 12–13px / 600, 흰색·보조 |
| 섹션 타이틀 | 15–16px / 700 |
| 통계 수치 | 18–20px / 800 |
| 다크 존 섹션 라벨 | 13px / 700, `--my-v2-ink-label` |

---

## 5. 간격 & 라운드

| 항목 | 값 |
|------|-----|
| 화면 좌우 패딩 | 16px |
| 프로필 카드 radius | 20px (`--radius-xl`) |
| 통계·메뉴 카드 radius | 16px (`--radius-lg`) |
| 아바타 | 80×80, 원형, 흰 링 |
| 통계 아이콘 원 | 36×36 |
| 섹션 간 | 16–20px |
| 다크 존 상단 | padding-top 20px, 상단 모서리 없음(스크롤 연속) |

---

## 6. MY-01 레이아웃 (v2)

```
.my-v2
├── .my-v2__scroll (밝은 surface)
│   ├── .my-v2-hero          ← 틸 프로필 카드
│   ├── .my-v2-xp            ← 경험치 (카드 밖)
│   ├── .my-v2-stats         ← 2×2 통계
│   ├── .my-v2-section       ← 최근 획득 카드 (가로 스크롤)
│   └── .my-v2-section       ← 나의 여행 기록 (타임라인)
└── .my-v2__footer           ← 다크 존
    ├── 내 활동 (활동 히스토리)
    └── 설정 (알림 / 도움말 / 약관·개인정보 / 버전 / 로그아웃)
```

### 6.1 프로필 히어로 (`.my-v2-hero`)

- 배경: `--my-v2-primary` 솔리드 (강한 그라데이션 대신 단색으로 시안과 맞춤)
- 상단 우측: 연필 아이콘만 (편집 진입) — 텍스트 버튼 제거로 시안 정렬
- 중앙: 흰 원형 아바타 + 사람 아이콘(또는 이니셜)
- 닉네임 → `Lv.N`
- 하단 행: 좌 `Gold Rank` 골드 pill / 우 `월간 N위` + `N점`

### 6.2 경험치 (`.my-v2-xp`)

- 라벨 “경험치” + 도움말(?) 아이콘
- 우측 `현재 / 최대 XP`
- 진행 바: 트랙 `--surface-dim`, fill `--my-v2-primary`, height 8px

### 6.3 통계 그리드 (`.my-v2-stats`)

| 셀 | 라벨 | 단위 예 |
|----|------|---------|
| 1 | 방문 지역 | N개 |
| 2 | 랜드마크 인증 | N회 |
| 3 | 획득한 뱃지 | N개 |
| 4 | 수집 카드 | N개 |

- 흰 카드 + 보더, 체크(fill) 아이콘을 soft 틸 원 안에 배치

### 6.4 최근 획득 카드 / 여행 기록

- 섹션 헤더: 제목 + `더보기 >`
- 카드: 기존 `.collect-card` 패턴 재사용, 가로 스크롤
- 여행 기록: 기존 `.cert-timeline--my` 재사용

### 6.5 다크 메뉴 존 (`.my-v2__footer`)

- 배경 `#121416` (순수 `#000` 지양 — DESIGN.md Don’t 준수)
- 리스트는 흰 카드 + 보더
- 로그아웃 라벨 색: `--my-v2-primary`
- 버전 행: 우측 `v1.0.0` 메타 텍스트

---

## 7. 컴포넌트 · 아이콘

- **아이콘 세트:** MingCute (`*-line` / 완료는 `*-fill`) — v1과 동일
- **통계:** `mingcute:check-circle-fill`
- **랭크:** `mingcute:trophy-fill`
- **편집:** `mingcute:edit-2-line`

---

## 8. 모션

v1 토큰 재사용:

- `--dur-fast` / `--ease-out`
- `.is-pressable:active` → `scale(0.97)`
- 히어로 진입: 약한 fade + `translateY(8px)` (선택)

---

## 9. Do / Don't (v2)

### Do

- 마이페이지 v2 UI는 `.my-v2` 안에서만 틸 토큰 사용
- 다크 존과 밝은 존의 대비로 정보 계층을 나눈다
- XP는 히어로 밖에 두어 “성장 게이지”를 독립 스캔 가능하게 한다

### Don't

- `DESIGN.md` / 전역 `--primary` 블루를 덮어쓰지 않는다
- 히어로 위에 플로팅 뱃지·스티커를 추가하지 않는다
- 다크 존에 순수 `#000` 또는 과도한 glow를 쓰지 않는다

---

## 10. 파일 훅

| 파일 | 역할 |
|------|------|
| `js/mypage.js` | `renderMain` (v1), `renderMainV2` (v2) |
| `css/screens.css` | `.my-v2*` 스타일 블록 |
| `index.html` | v1 / v2 폰 프레임 비교 쇼케이스 |
| `DESIGN.md` | **변경하지 않음** (v1 기준 문서) |

---

## 11. 시안 대비 소폭 수정 메모

레퍼런스 이미지 대비 프로토타입에 맞춘 조정:

1. 닉네임·레벨·점수는 기존 더미 프로필(`여행하는성훈` 등)을 유지해 v1과 수치 비교 가능
2. 하단 배경을 순黑 대신 `#121416`으로 완화
3. 설정에 기존 앱 플로우(`data-go`)를 연결 (알림·도움말·로그아웃)
4. 카드/타임라인은 기존 컴포넌트를 재사용해 디자인 시스템 파편화 방지

---

*작성: 마이페이지 개편 비교 프로토타입 · DESIGN.md는 원본 유지*
